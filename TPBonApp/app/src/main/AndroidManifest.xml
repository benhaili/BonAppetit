<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tpbonapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PanierActivity"></activity>
        <activity android:name=".FoodDetail" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ListProduit" />
        <activity
            android:name=".ui.login.ListeProduit"
            android:label="@string/title_activity_liste_produit" />
        <activity android:name=".MenuCategories" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>